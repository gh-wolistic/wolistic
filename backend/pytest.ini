[pytest]
pythonpath = .
testpaths = tests
addopts = -q
markers =
    smoke: E2E smoke tests that must pass before deploy (deselect with '-m "not smoke"')
    asyncio: mark test as async
    integration: mark test as integration test
